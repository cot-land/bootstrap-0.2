var g_counter: i64 = 0;
fn increment() void { g_counter = g_counter + 1; }
fn get_counter() i64 { return g_counter; }
test "function: global access" {
    increment();
    increment();
    increment();
    @assert(get_counter() == 3)
}
