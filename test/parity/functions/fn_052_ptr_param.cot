fn swap(a: *i64, b: *i64) void {
    var tmp: i64 = a.*;
    a.* = b.*;
    b.* = tmp;
}
test "function: ptr param" {
    var x: i64 = 10;
    var y: i64 = 20;
    swap(&x, &y);
    @assert(x == 20 and y == 10)
}
