fn sum_range(a: i64, b: i64) i64 {
    var sum: i64 = 0;
    var i: i64 = a;
    while i <= b {
        sum = sum + i;
        i = i + 1;
    }
    return sum;
}
test "function: sum range" {
    @assert(sum_range(5, 10) == 45)
}
