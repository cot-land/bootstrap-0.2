fn isqrt(n: i64) i64 {
    var x: i64 = n;
    var y: i64 = (x + 1) / 2;
    while y < x {
        x = y;
        y = (x + n / x) / 2;
    }
    return x;
}
test "function: integer sqrt" {
    @assert(isqrt(100) == 10)
}
