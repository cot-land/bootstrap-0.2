extern fn malloc_i64(count: i64) *i64;
fn increment(ptr: *i64) void {
    ptr.* = ptr.* + 1;
}
test "memory: increment via ptr" {
    var x: *i64 = malloc_i64(1);
    x.* = 41;
    increment(x);
    @assert(x.* == 42)
}
