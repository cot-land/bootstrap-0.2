extern fn malloc_i64(count: i64) *i64;
fn swap_ptr(a: *i64, b: *i64) void {
    var t: i64 = a.*;
    a.* = b.*;
    b.* = t;
}
test "memory: swap via ptr" {
    var x: *i64 = malloc_i64(1);
    var y: *i64 = malloc_i64(1);
    x.* = 10;
    y.* = 20;
    swap_ptr(x, y);
    @assert(x.* == 20 and y.* == 10)
}
