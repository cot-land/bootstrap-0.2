extern fn malloc_i64(count: i64) *i64;
fn add_to(ptr: *i64, val: i64) void {
    ptr.* = ptr.* + val;
}
test "memory: ptr param" {
    var x: *i64 = malloc_i64(1);
    x.* = 30;
    add_to(x, 12);
    @assert(x.* == 42)
}
