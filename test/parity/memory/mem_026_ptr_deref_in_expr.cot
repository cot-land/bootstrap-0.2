extern fn malloc_i64(count: i64) *i64;
test "memory: ptr deref in expr" {
    var arr: *i64 = malloc_i64(3);
    (arr + 0).* = 10;
    (arr + 1).* = 20;
    (arr + 2).* = 30;
    var result: i64 = (arr + 0).* + (arr + 1).* * 2 + (arr + 2).* / 3;
    @assert(result == 60)
}
