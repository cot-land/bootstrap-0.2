extern fn malloc_i64(count: i64) *i64;
fn reverse(arr: *i64, len: i64) void {
    var i: i64 = 0;
    var j: i64 = len - 1;
    while i < j {
        var tmp: i64 = (arr + i).*;
        (arr + i).* = (arr + j).*;
        (arr + j).* = tmp;
        i = i + 1;
        j = j - 1;
    }
}
test "array: reverse" {
    var arr: *i64 = malloc_i64(5);
    (arr + 0).* = 1;
    (arr + 1).* = 2;
    (arr + 2).* = 3;
    (arr + 3).* = 4;
    (arr + 4).* = 5;
    reverse(arr, 5);
    @assert((arr + 0).* == 5 and (arr + 4).* == 1)
}
