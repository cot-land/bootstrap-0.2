extern fn malloc_i64(count: i64) *i64;
test "array: index calc" {
    var arr: *i64 = malloc_i64(5);
    var i: i64 = 0;
    while i < 5 {
        (arr + i).* = i * 10;
        i = i + 1;
    }
    @assert((arr + 0).* == 0 and (arr + 4).* == 40)
}
