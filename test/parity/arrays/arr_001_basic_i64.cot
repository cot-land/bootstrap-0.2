extern fn malloc_i64(count: i64) *i64;
test "array: basic i64" {
    var arr: *i64 = malloc_i64(4);
    (arr + 0).* = 10;
    (arr + 1).* = 20;
    (arr + 2).* = 30;
    (arr + 3).* = 40;
    var sum: i64 = (arr + 0).* + (arr + 1).* + (arr + 2).* + (arr + 3).*;
    @assert(sum == 100)
}
