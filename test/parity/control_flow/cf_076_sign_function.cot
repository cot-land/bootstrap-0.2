fn sign(x: i64) i64 {
    if x > 0 { return 1; }
    if x < 0 { return 0 - 1; }
    return 0;
}
test "control flow: sign function" {
    @assert(sign(42) == 1 and sign(0-5) == 0-1 and sign(0) == 0)
}
