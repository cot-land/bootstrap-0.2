fn count_divisible(n: i64, d: i64) i64 {
    var count: i64 = 0;
    var i: i64 = 1;
    while i <= n {
        if i % d == 0 {
            count = count + 1;
        }
        i = i + 1;
    }
    return count;
}
test "control flow: count loop" {
    @assert(count_divisible(20, 3) == 6)
}
