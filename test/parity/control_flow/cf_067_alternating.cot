test "control flow: alternating" {
    var sum: i64 = 0;
    var i: i64 = 1;
    var sign: i64 = 1;
    while i <= 10 {
        sum = sum + sign * i;
        sign = 0 - sign;
        i = i + 1;
    }
    @assert(sum == 0 - 5)
}
