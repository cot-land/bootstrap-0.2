fn first_divisible(n: i64, d1: i64, d2: i64) i64 {
    var i: i64 = 1;
    while i <= n {
        if (i % d1) == 0 { break; }
        if (i % d2) == 0 { break; }
        i = i + 1;
    }
    return i;
}
test "control flow: multiple breaks" {
    @assert(first_divisible(20, 7, 11) == 7)
}
