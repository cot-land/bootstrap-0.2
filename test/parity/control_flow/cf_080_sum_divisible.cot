fn sum_divisible(n: i64, d: i64) i64 {
    var sum: i64 = 0;
    var i: i64 = 1;
    while i <= n {
        if i % d == 0 {
            sum = sum + i;
        }
        i = i + 1;
    }
    return sum;
}
test "control flow: sum divisible" {
    @assert(sum_divisible(10, 2) == 30)
}
