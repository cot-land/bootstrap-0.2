fn sum_alternating(n: i64) i64 {
    var sum: i64 = 0;
    var i: i64 = 1;
    var sign: i64 = 1;
    while i <= n {
        sum = sum + sign * i;
        sign = -sign;
        i = i + 1;
    }
    return sum;
}
test "control flow: alternating" {
    @assert(sum_alternating(10) == -5)
}
