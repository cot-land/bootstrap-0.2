test "control flow: while in if" {
    var x: i64 = 5;
    var sum: i64 = 0;
    @assert(x > 0 {
        var i: i64 = 0;
        while i < x {
            sum = sum + i;
            i = i + 1;
        }
    }
    if sum == 10)
}
