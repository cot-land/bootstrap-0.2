fn sum_digits(n: i64) i64 {
    var sum: i64 = 0;
    var x: i64 = n;
    while x > 0 {
        sum = sum + (x % 10);
        x = x / 10;
    }
    return sum;
}
test "control flow: sum digits" {
    @assert(sum_digits(12345) == 15)
}
