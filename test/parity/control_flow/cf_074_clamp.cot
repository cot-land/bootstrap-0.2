fn clamp(x: i64, lo: i64, hi: i64) i64 {
    if x < lo { return lo; }
    if x > hi { return hi; }
    return x;
}
test "control flow: clamp" {
    @assert(clamp(5, 0, 10) == 5 and clamp(0-5, 0, 10) == 0 and clamp(15, 0, 10) == 10)
}
