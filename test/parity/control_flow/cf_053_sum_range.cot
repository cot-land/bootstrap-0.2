fn sum_range(start: i64, end: i64) i64 {
    var sum: i64 = 0;
    var i: i64 = start;
    while i <= end {
        sum = sum + i;
        i = i + 1;
    }
    return sum;
}
test "control flow: sum range" {
    @assert(sum_range(1, 100) == 5050)
}
