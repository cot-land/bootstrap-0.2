struct Point { x: i64, y: i64 }
fn translate(p: *Point, dx: i64, dy: i64) void {
    p.x = p.x + dx;
    p.y = p.y + dy;
}
test "type: struct fn modify" {
    var p: Point = undefined;
    p.x = 10;
    p.y = 20;
    translate(&p, 5, 10);
    @assert(p.x == 15 and p.y == 30)
}
