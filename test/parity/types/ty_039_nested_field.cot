struct Inner { value: i64 }
struct Outer { inner: Inner, count: i64 }
test "type: nested field" {
    var o: Outer = undefined;
    o.inner.value = 42;
    o.count = 10;
    @assert(o.inner.value == 42 and o.count == 10)
}
