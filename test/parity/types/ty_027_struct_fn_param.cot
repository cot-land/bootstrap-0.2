struct Point { x: i64, y: i64 }
fn distance_sq(p: *Point) i64 {
    return p.x * p.x + p.y * p.y;
}
test "type: struct fn param" {
    var p: Point = undefined;
    p.x = 3;
    p.y = 4;
    @assert(distance_sq(&p) == 25)
}
