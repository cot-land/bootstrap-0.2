extern fn malloc_i64(count: i64) *i64;
test "type: ptr arithmetic" {
    var arr: *i64 = malloc_i64(3);
    (arr + 0).* = 10;
    (arr + 1).* = 20;
    (arr + 2).* = 30;
    var p: *i64 = arr + 1;
    @assert(p.* == 20)
}
