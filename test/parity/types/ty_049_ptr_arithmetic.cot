extern fn malloc_i64(count: i64) *i64;
fn main() i64 {
    var arr: *i64 = malloc_i64(3);
    (arr + 0).* = 10;
    (arr + 1).* = 20;
    (arr + 2).* = 30;
    var p: *i64 = arr + 1;
    if p.* == 20 { return 0; }
    return 1;
}
