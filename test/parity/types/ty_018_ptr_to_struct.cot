struct Data { value: i64 }
fn get_value(p: *Data) i64 { return p.value; }
fn set_value(p: *Data, v: i64) void { p.value = v; }
test "type: ptr to struct" {
    var d: Data = undefined;
    set_value(&d, 42);
    @assert(get_value(&d) == 42)
}
