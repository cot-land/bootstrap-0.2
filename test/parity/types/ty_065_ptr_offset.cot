extern fn malloc_i64(count: i64) *i64;
fn main() i64 {
    var arr: *i64 = malloc_i64(5);
    (arr + 0).* = 100;
    (arr + 1).* = 200;
    (arr + 2).* = 300;
    var p: *i64 = arr;
    p = p + 2;
    if p.* == 300 { return 0; }
    return 1;
}
