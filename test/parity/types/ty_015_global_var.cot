// NOTE: Zig compiler has bug with global initialization
// Work around by initializing in main()
var g_value: i64 = 0;
fn modify_global() void { g_value = g_value + 50; }
test "type: global var" {
    g_value = 100;
    modify_global();
    @assert(g_value == 150)
}
