struct Inner { val: i64 }
struct Outer { inner: Inner, extra: i64 }
test "type: nested struct" {
    var o: Outer = undefined;
    o.inner.val = 100;
    o.extra = 5;
    @assert(o.inner.val + o.extra == 105)
}
