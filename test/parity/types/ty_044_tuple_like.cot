struct Pair { first: i64, second: i64 }
fn make_pair(a: i64, b: i64, p: *Pair) void {
    p.first = a;
    p.second = b;
}
test "type: tuple like" {
    var p: Pair = undefined;
    make_pair(10, 20, &p);
    @assert(p.first == 10 and p.second == 20)
}
